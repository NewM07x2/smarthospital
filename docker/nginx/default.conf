server {
    listen 80;
    server_name localhost;
    root /var/www/html/public;
    index index.php index.html index.htm;

    # ★ /api 以外のルートは Laravelの index.php へ渡す
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # ★★★ APIルート (/api) 専用の設定 ★★★
    location /api {
        # CORSヘッダーをAPIリクエスト全体に適用
        add_header 'Access-Control-Allow-Origin' 'http://localhost:3000' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
        add_header 'Access-Control-Allow-Headers' 'Accept, Authorization, Content-Type, X-Requested-With, X-CSRF-TOKEN' always;
        add_header 'Access-Control-Allow-Credentials' 'true' always;
        # OPTIONSリクエストの処理ブロックは削除！

        # APIリクエストをLaravelのindex.phpへプロキシ
        try_files $uri $uri/ /index.php?$query_string;
    }

    # PHP-FPMとの連携（API以外も含む）
    location ~ \.php$ {
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;
    }

    # 静的ファイルの配信
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # セキュリティ設定
    location ~ /\.ht {
        deny all;
    }
}